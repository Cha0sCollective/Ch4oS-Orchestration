# Current work

No active Ch4oS-Installer work packet is recorded in this orchestration overlay.
Do not infer one from prior chat or the next-priority list in `docs/STATUS.md`.

For a new packet, verify the target repository's actual branch, `HEAD`, dirty state
and issue or owner instruction. Use `docs/STATUS.md` for current implementation and
validation, then follow the target `AGENTS.md` required-reading list. Dated status
is evidence and orientation, not standing authorization.

Local-agent service implementation is active only in Ch4oS-Orchestration. It does
not change this project's effective agents. Adoption requires a separate reviewed
target change and owner approval.
