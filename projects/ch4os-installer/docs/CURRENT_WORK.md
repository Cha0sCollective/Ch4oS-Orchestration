# Current work

No active Ch4oS-Installer work packet is recorded in this orchestration overlay.
Do not infer one from prior chat or the next-priority list in `docs/STATUS.md`.

For a new packet, verify the target repository's actual branch, `HEAD`, dirty state
and issue or owner instruction. Use `docs/STATUS.md` for current implementation and
validation, then follow the target `AGENTS.md` required-reading list. Dated status
is evidence and orientation, not standing authorization.

The prepared coordinator-guidance adoption is paused. It does not change this
project's effective agents until separately reviewed and approved in the target
repository. There is no active product packet and no authority to merge or proceed
automatically.

Local-worker trials are stopped. Worker service 0.1.6 is installed and registered
on the host. The exact NVIDIA Nemotron 3 Ultra free profiles require no
qualification: `remote-analyst` is available for summaries and
`remote-comparison` for research in ordinary `work` mode. Use capabilities
`availableTaskClasses`, explicit remote consent and configured repository scope,
then verify every result. Nemotron Ultra jobs/results have no age-based expiry but
remain subject to count and storage-size bounds. Other profiles remain unavailable
without qualification and retain the 24-hour job/result lifetime.
The earlier local Gemma summary result remains 6/6 historical evidence. See
[Local tools](LOCAL_TOOLS.md) for the concise installed-capability summary.
