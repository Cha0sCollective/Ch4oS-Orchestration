# Current work

No active Create: Ch4oS work packet is recorded in this orchestration overlay. Do
not infer one from prior chat, the latest release notes or deferred checks.

For a new packet, verify the target repository's actual branch, `HEAD`, dirty state
and issue or owner instruction. Begin with `docs/PRODUCT-CHARTER.md` and
`docs/P0-BASELINE.md`, then follow the target `AGENTS.md` required-reading list.
The baseline records accepted behavior and outstanding observations without making
those observations a new assignment.

Local-agent service implementation is active only in Ch4oS-Orchestration. It does
not change this project's effective agents. Adoption requires a separate reviewed
target change and owner approval.
